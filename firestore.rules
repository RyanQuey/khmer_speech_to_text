rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // propagates rules down all subcollections
    match /{document=**} {
      function email() {
        return request.auth.token.email;
      }

      // require users to have a record in whitelist (see firestore db) or variations on my own email to continue
      function isWhitelisted() {
        return email().matches('rlquey2.*@gmail[.]com') == true || exists(/databases/$(database)/documents/whitelistedUsers/$(email()));
      }

      allow read: if isWhitelisted();
      allow write: if isWhitelisted();
    } 
  }
}
